<local:CWindow x:Class="ModCreator.MainWindow" x:TypeArguments="localData:MainWindowData"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:ModCreator.Windows"
        xmlns:localData="clr-namespace:ModCreator.WindowData" xmlns:helper="clr-namespace:ModCreator.Helpers"
        xmlns:converters="clr-namespace:ModCreator.Converters" mc:Ignorable="d"
        Title="{x:Static helper:MainWindowText.AppTitle}" Height="600" Width="1000" WindowStartupLocation="CenterScreen"
        WindowState="Maximized" Icon="/Resources/app-icon.png" FontFamily="Segoe UI">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        <converters:ProjectStateToBackgroundConverter x:Key="ProjectStateToBackgroundConverter"/>
        <converters:ProjectStateToTextConverter x:Key="ProjectStateToTextConverter"/>
        <converters:ProjectStateToIsEnabledConverter x:Key="ProjectStateToIsEnabledConverter"/>
        <converters:FilePathToImageConverter x:Key="FilePathToImageConverter"/>
        <Style TargetType="GroupBox"><Setter Property="Padding" Value="10"/><Setter Property="Margin" Value="5"/></Style>
        <Style TargetType="TextBlock" x:Key="LabelText"><Setter Property="FontWeight" Value="SemiBold"/><Setter Property="Margin" Value="0,5,10,5"/><Setter Property="VerticalAlignment" Value="Center"/></Style>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- Header -->
        <Border Grid.Row="0" Background="#FF2E5090" Padding="15" Margin="0,0,0,10" CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="{x:Static helper:MainWindowText.AppName}" FontSize="24" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="{x:Static helper:MainWindowText.AppSubtitle}" FontSize="12" Foreground="#FFCCCCCC" Margin="0,5,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="{x:Static helper:MainWindowText.Help}" Click="Help_Click" Style="{StaticResource HeaderButton}" Width="70" Margin="0,0,5,0"/>
                    <Button Content="{x:Static helper:MainWindowText.About}" Click="About_Click" Style="{StaticResource HeaderButton}" Width="70" Margin="5,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
        <!-- Workplace Setting -->
        <GroupBox Grid.Row="1" Header="{x:Static helper:MainWindowText.Workplace}" Margin="0,0,0,10">
            <DockPanel>
                <Button Content="{x:Static helper:MainWindowText.Browse}" Click="BrowseWorkplace_Click" Style="{StaticResource ActionButton}" Width="80" DockPanel.Dock="Right" Margin="10,0,0,0"/>
                <TextBox Text="{Binding WorkplacePath, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" Style="{StaticResource StandardTextBox}" DockPanel.Dock="Left"/>
            </DockPanel>
        </GroupBox>
        <!-- Main Content -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="400"/></Grid.ColumnDefinitions>
            <!-- Projects List -->
            <GroupBox Grid.Column="0" Header="{x:Static helper:MainWindowText.ProjectList}">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <!-- Toolbar -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="{x:Static helper:MainWindowText.CreateNewProject}" Style="{StaticResource PrimaryButton}" Click="CreateProject_Click" Foreground="White"/>
                        <Button Content="{x:Static helper:MainWindowText.RefreshList}" Style="{StaticResource ActionButton}" Click="RefreshProjects_Click"/>
                        <TextBlock Grid.Column="0" Text="🔍" FontSize="16" VerticalAlignment="Center" Margin="40,0,10,0"/>
                        <TextBox Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="5" VerticalContentAlignment="Center">
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Setter Property="Padding" Value="5"/>
                                    <Style.Triggers>
                                        <Trigger Property="Text" Value="">
                                            <Setter Property="Background">
                                                <Setter.Value>
                                                    <VisualBrush Stretch="None" AlignmentX="Left">
                                                        <VisualBrush.Visual>
                                                            <TextBlock Text="{x:Static helper:MainWindowText.SearchPlaceholder}" Foreground="Gray" Margin="5,0,0,0"/>
                                                        </VisualBrush.Visual>
                                                    </VisualBrush>
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                    </StackPanel>
                    <!-- Project List -->
                    <DataGrid Grid.Row="1" ItemsSource="{Binding FilteredProjects, Mode=OneWay}" SelectedItem="{Binding SelectedProject, Mode=TwoWay}" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" CanUserResizeRows="False" RowHeight="40" ColumnHeaderHeight="48" AlternatingRowBackground="#FFF5F5F5" MouseDoubleClick="ProjectList_DoubleClick" CellStyle="{StaticResource ProjectGridCellStyle}" ColumnHeaderStyle="{StaticResource ProjectGridHeaderStyle}">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridRow">
                                            <Border x:Name="DGR_Border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="True">
                                                <Grid>
                                                    <Border x:Name="BaseBackground" Background="{TemplateBinding Background}"/>
                                                    <Border x:Name="StateOverlay" Background="Transparent" Opacity="0.15"/>
                                                    <Border x:Name="HoverOverlay" Background="#FF2E5090" Opacity="0"/>
                                                    <Border x:Name="SelectionHighlight" Background="#FF2E5090" Opacity="0"/>
                                                    <!-- Content -->
                                                    <SelectiveScrollingGrid>
                                                        <SelectiveScrollingGrid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/>
                                                        </SelectiveScrollingGrid.ColumnDefinitions>
                                                        <SelectiveScrollingGrid.RowDefinitions>
                                                            <RowDefinition Height="*"/><RowDefinition Height="Auto"/>
                                                        </SelectiveScrollingGrid.RowDefinitions>
                                                        <DataGridCellsPresenter Grid.Column="1" ItemsPanel="{TemplateBinding ItemsPanel}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                                        <DataGridDetailsPresenter Grid.Column="1" Grid.Row="1" SelectiveScrollingGrid.SelectiveScrollingOrientation="{Binding AreRowDetailsFrozen, ConverterParameter={x:Static SelectiveScrollingOrientation.Vertical}, Converter={x:Static DataGrid.RowDetailsScrollingConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" Visibility="{TemplateBinding DetailsVisibility}"/>
                                                        <DataGridRowHeader Grid.RowSpan="2" SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical" Visibility="{Binding HeadersVisibility, ConverterParameter={x:Static DataGridHeadersVisibility.Row}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"/>
                                                    </SelectiveScrollingGrid>
                                                </Grid>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <DataTrigger Binding="{Binding State}" Value="ProjectNotFound">
                                                    <Setter TargetName="StateOverlay" Property="Background" Value="#FFFF0000"/>
                                                </DataTrigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Trigger.EnterActions>
                                                        <BeginStoryboard><Storyboard><DoubleAnimation Storyboard.TargetName="HoverOverlay" Storyboard.TargetProperty="Opacity" To="0.08" Duration="0:0:0.2"/></Storyboard></BeginStoryboard>
                                                    </Trigger.EnterActions>
                                                    <Trigger.ExitActions>
                                                        <BeginStoryboard><Storyboard><DoubleAnimation Storyboard.TargetName="HoverOverlay" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/></Storyboard></BeginStoryboard>
                                                    </Trigger.ExitActions>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="SelectionHighlight" Property="Opacity" Value="0.15"/>
                                                </Trigger>
                                                <Trigger Property="AlternationIndex" Value="1">
                                                    <Setter TargetName="BaseBackground" Property="Background" Value="#FFF5F5F5"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{x:Static helper:MainWindowText.HeaderProjectName}" Binding="{Binding ProjectName, Mode=OneWay}" Width="200" MinWidth="150" ElementStyle="{StaticResource LeftTextBlockStyle}"/>
                            <DataGridTextColumn Header="{x:Static helper:MainWindowText.HeaderDescription}" Binding="{Binding Description, Mode=OneWay}" Width="*" MinWidth="200" ElementStyle="{StaticResource LeftTextBlockStyle}"/>
                            <DataGridTextColumn Header="{x:Static helper:MainWindowText.HeaderId}" Binding="{Binding ProjectId, Mode=OneWay}" Width="100" ElementStyle="{StaticResource CenterTextBlockStyle}"/>
                            <DataGridTextColumn Header="{x:Static helper:MainWindowText.HeaderState}" Binding="{Binding State, Mode=OneWay, Converter={StaticResource ProjectStateToTextConverter}}" Width="100" ElementStyle="{StaticResource CenterTextBlockStyle}"/>
                            <DataGridTextColumn Header="{x:Static helper:MainWindowText.HeaderCreated}" Binding="{Binding CreatedDate, Mode=OneWay, StringFormat=\{0:yyyy-MM-dd\}}" Width="110" ElementStyle="{StaticResource CenterTextBlockStyle}"/>
                            <DataGridTextColumn Header="{x:Static helper:MainWindowText.HeaderModified}" Binding="{Binding LastModifiedDate, Mode=OneWay, StringFormat=\{0:yyyy-MM-dd\}}" Width="110" ElementStyle="{StaticResource CenterTextBlockStyle}"/>
                            <DataGridTemplateColumn Header="{x:Static helper:MainWindowText.HeaderActions}" Width="110">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="{x:Static helper:MainWindowText.GridOpenFolder}" Background="Transparent" BorderThickness="0" ToolTip="{x:Static helper:MainWindowText.TooltipOpenFolder}" Click="GridOpenFolder_Click" IsEnabled="{Binding State, Converter={StaticResource ProjectStateToIsEnabledConverter}}" Width="30" Margin="2" Padding="2"/>
                                            <Button Content="{x:Static helper:MainWindowText.GridEditInfo}" Background="Transparent" BorderThickness="0" ToolTip="{x:Static helper:MainWindowText.TooltipEditInfo}" Click="GridEditProject_Click" IsEnabled="{Binding State, Converter={StaticResource ProjectStateToIsEnabledConverter}}" Width="30" Margin="2" Padding="2"/>
                                            <Button Content="{x:Static helper:MainWindowText.GridDelete}" Background="Transparent" BorderThickness="0" ToolTip="{x:Static helper:MainWindowText.TooltipDelete}" Click="GridDeleteProject_Click" Width="30" Margin="2" Padding="2" Foreground="Red"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </GroupBox>
            <!-- Project Details & Actions -->
            <StackPanel Grid.Column="1">
                <GroupBox Header="{x:Static helper:MainWindowText.ProjectDetails}">
                    <StackPanel x:Name="pnlProjectDetails">
                        <TextBlock Text="{x:Static helper:MainWindowText.NoProjectSelected}" x:Name="txtNoSelection" Foreground="Gray" HorizontalAlignment="Center" Margin="20" Visibility="{Binding HasNoSelectedProject, Converter={StaticResource BoolToVisConverter}}"/>
                        <StackPanel x:Name="pnlDetails" Visibility="{Binding HasSelectedProject, Converter={StaticResource BoolToVisConverter}}">
                            <StackPanel Orientation="Horizontal" Margin="0,5">
                                <TextBlock Text="{x:Static helper:MainWindowText.ProjectDetailsName}" Style="{StaticResource LabelText}" Width="100"/>
                                <TextBlock Text="{Binding ProjectName, Mode=OneWay}" TextWrapping="Wrap"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,5">
                                <TextBlock Text="{x:Static helper:MainWindowText.ProjectDetailsId}" Style="{StaticResource LabelText}" Width="100"/>
                                <TextBlock Text="{Binding ProjectId, Mode=OneWay}" TextWrapping="Wrap"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,5">
                                <TextBlock Text="{x:Static helper:MainWindowText.ProjectDetailsAuthor}" Style="{StaticResource LabelText}" Width="100"/>
                                <TextBlock Text="{Binding Author, Mode=OneWay}" TextWrapping="Wrap"/>
                            </StackPanel>
                            <StackPanel Margin="0,5">
                                <TextBlock Text="{x:Static helper:MainWindowText.ProjectDetailsPath}" Style="{StaticResource LabelText}"/>
                                <TextBlock Text="{Binding ProjectPath, Mode=OneWay}" TextWrapping="Wrap" Margin="5"/>
                            </StackPanel>
                            <StackPanel Margin="0,5">
                                <TextBlock Text="{x:Static helper:MainWindowText.ProjectDetailsDescription}" Style="{StaticResource LabelText}"/>
                                <TextBlock Text="{Binding Description, Mode=OneWay}" TextWrapping="Wrap" Margin="5"/>
                            </StackPanel>
                            <Border BorderBrush="#CCCCCC" BorderThickness="1" Margin="0,10,0,0" CornerRadius="3">
                                <Image Source="{Binding TitleImg, Mode=OneWay, Converter={StaticResource FilePathToImageConverter}}" Stretch="Uniform" MaxHeight="300" Margin="5">
                                    <Image.Style>
                                        <Style TargetType="Image">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding TitleImg, Converter={StaticResource FilePathToImageConverter}}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Image.Style>
                                </Image>
                            </Border>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>
                <GroupBox Header="{x:Static helper:MainWindowText.Actions}">
                    <StackPanel x:Name="pnlActions" IsEnabled="{Binding IsSelectedProjectValid}">
                        <Button Content="{x:Static helper:MainWindowText.OpenFolder}" Click="OpenFolder_Click" Margin="5"/>
                        <Button Content="{x:Static helper:MainWindowText.EditInfo}" Click="EditProject_Click" Margin="5"/>
                        <Separator Margin="5,10"/>
                        <Button Content="{x:Static helper:MainWindowText.RemoveProject}" Click="DeleteProject_Click" Margin="5"/>
                    </StackPanel>
                </GroupBox>
                <GroupBox Header="{x:Static helper:MainWindowText.Info}">
                    <StackPanel>
                        <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="Gray">
                            <Run Text="{x:Static helper:MainWindowText.TotalProjects}"/>
                            <Run Text="{Binding TotalCount, Mode=OneWay}" FontWeight="Bold"/>
                        </TextBlock>
                        <TextBlock Text="{x:Static helper:MainWindowText.Template}" FontSize="11" Foreground="Gray" Margin="0,5,0,0"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </Grid>
        <!-- Status Bar -->
        <Border Grid.Row="3" Background="#FFF0F0F0" Padding="10,5" Margin="0,10,0,0" CornerRadius="3">
            <TextBlock Text="{Binding StatusMessage, Mode=OneWay}" FontSize="11"/>
        </Border>
    </Grid>
</local:CWindow>