<local:CWindow x:Class="ModCreator.Windows.NewProjectWindow"
        x:TypeArguments="localData:NewProjectWindowData"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ModCreator.Windows"
        xmlns:localData="clr-namespace:ModCreator.WindowData"
        xmlns:helper="clr-namespace:ModCreator.Helpers"
        mc:Ignorable="d"
        Title="{x:Static helper:NewProjectWindowText.NewProjectTitle}" 
        Height="350" Width="550"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Icon="/Resources/app-icon.png"
        FontFamily="Segoe UI">
    
    <Border Margin="16" Padding="8" Background="White" CornerRadius="8">
        <Grid>
                <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Author -->
                <StackPanel Grid.Row="0">
                <TextBlock Text="{x:Static helper:NewProjectWindowText.ProjectFieldAuthor}" Style="{StaticResource LabelText}" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Author, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                    Style="{StaticResource StandardTextBox}" Height="32"/>
                </StackPanel>

                <!-- Project Name -->
                <StackPanel Grid.Row="1">
                <TextBlock Text="{x:Static helper:NewProjectWindowText.ProjectFieldName}" Style="{StaticResource LabelText}" Margin="0,0,0,4"/>
                <TextBox
                     Text="{Binding ProjectName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     Style="{StaticResource StandardTextBox}" Height="32"/>
                </StackPanel>

                <!-- Description -->
                <StackPanel Grid.Row="2">
                <TextBlock Text="{x:Static helper:NewProjectWindowText.ProjectFieldDescription}" Style="{StaticResource LabelText}" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                    Style="{StaticResource MultilineTextBox}" Height="60"/>
                </StackPanel>

                <!-- Buttons -->
                <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
                <Button Content="{x:Static helper:NewProjectWindowText.Create}" 
                    Click="CreateButton_Click"
                    Style="{StaticResource PrimaryButton}"
                    IsEnabled="{Binding CanCreate, Mode=OneWay}"
                    Width="100"/>
                <Button Content="{x:Static helper:NewProjectWindowText.Cancel}" 
                    Click="CancelButton_Click"
                    Style="{StaticResource ActionButton}"
                    Width="100"/>
            </StackPanel>
        </Grid>
    </Border>
</local:CWindow>
