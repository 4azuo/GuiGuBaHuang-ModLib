<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Merge Colors -->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Colors.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- Base TextBox Style -->
    <Style TargetType="TextBox" x:Key="BaseTextBox">
        <Setter Property="Padding" Value="8,5"/>
        <Setter Property="Height" Value="30"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <!-- Standard TextBox -->
    <Style TargetType="TextBox" x:Key="StandardTextBox" BasedOn="{StaticResource BaseTextBox}">
        <Setter Property="Margin" Value="0,5"/>
    </Style>

    <!-- Search TextBox -->
    <Style TargetType="TextBox" x:Key="SearchTextBox" BasedOn="{StaticResource BaseTextBox}">
        <Setter Property="MinWidth" Value="150"/>
    </Style>

    <!-- Multiline TextBox -->
    <Style TargetType="TextBox" x:Key="MultilineTextBox" BasedOn="{StaticResource BaseTextBox}">
        <Setter Property="AcceptsReturn" Value="True"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="MinHeight" Value="60"/>
        <Setter Property="VerticalContentAlignment" Value="Top"/>
    </Style>

    <!-- ReadOnly TextBox -->
    <Style TargetType="TextBox" x:Key="ReadOnlyTextBox" BasedOn="{StaticResource BaseTextBox}">
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="Background" Value="{StaticResource BackgroundColor}"/>
    </Style>

</ResourceDictionary>
