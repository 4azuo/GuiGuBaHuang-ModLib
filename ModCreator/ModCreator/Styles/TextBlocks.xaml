<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Merge Colors -->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Colors.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- Base TextBlock Style -->
    <Style TargetType="TextBlock" x:Key="BaseText">
        <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
        <Setter Property="TextWrapping" Value="NoWrap"/>
    </Style>

    <!-- Header Text -->
    <Style TargetType="TextBlock" x:Key="HeaderText" BasedOn="{StaticResource BaseText}">
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{StaticResource TextLightColor}"/>
    </Style>

    <!-- SubHeader Text -->
    <Style TargetType="TextBlock" x:Key="SubHeaderText" BasedOn="{StaticResource BaseText}">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryLightColor}"/>
        <Setter Property="Margin" Value="0,5,0,0"/>
    </Style>

    <!-- Label Text -->
    <Style TargetType="TextBlock" x:Key="LabelText" BasedOn="{StaticResource BaseText}">
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Margin" Value="0,5,10,5"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="12"/>
    </Style>

    <!-- Info Text -->
    <Style TargetType="TextBlock" x:Key="InfoText" BasedOn="{StaticResource BaseText}">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Foreground" Value="{StaticResource TextMutedColor}"/>
    </Style>

    <!-- Body Text -->
    <Style TargetType="TextBlock" x:Key="BodyText" BasedOn="{StaticResource BaseText}">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <!-- Error Text -->
    <Style TargetType="TextBlock" x:Key="ErrorText" BasedOn="{StaticResource BaseText}">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Foreground" Value="{StaticResource DangerColor}"/>
        <Setter Property="FontStyle" Value="Italic"/>
    </Style>

</ResourceDictionary>
