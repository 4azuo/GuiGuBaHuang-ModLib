<local:CWindow x:Class="ModCreator.Windows.AddConfWindow" x:TypeArguments="localData:AddConfWindowData"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:ModCreator.Windows"
        xmlns:localData="clr-namespace:ModCreator.WindowData" xmlns:helper="clr-namespace:ModCreator.Helpers" mc:Ignorable="d"
        Title="{x:Static helper:AddConfWindowText.AddConfTitle}" Height="600" Width="800" WindowStartupLocation="CenterOwner"
        Icon="/Resources/app-icon.png" FontFamily="Segoe UI">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#FF2E5090" Padding="15">
            <TextBlock Text="{x:Static helper:AddConfWindowText.AddConfTitle}" FontSize="18" FontWeight="Bold" Foreground="White"/>
        </Border>
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="🔍" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" BorderBrush="#FFCCCCCC" BorderThickness="1" Padding="8" FontSize="13">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Style.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <VisualBrush Stretch="None" AlignmentX="Left">
                                            <VisualBrush.Visual>
                                                <TextBlock Text="{x:Static helper:AddConfWindowText.SearchPlaceholder}" Foreground="Gray" Margin="8,0,0,0"/>
                                            </VisualBrush.Visual>
                                        </VisualBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </Grid>
        <Grid Grid.Row="2" Margin="10">
            <Grid.ColumnDefinitions><ColumnDefinition Width="250"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <GroupBox Grid.Column="0" Header="{x:Static helper:AddConfWindowText.SelectConf}" Margin="0,0,10,0">
                <ListBox ItemsSource="{Binding FilteredConfigs}" SelectedItem="{Binding SelectedConfig, Mode=TwoWay}" DisplayMemberPath="Name" ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>
            </GroupBox>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <GroupBox Grid.Row="0" Header="{x:Static helper:AddConfWindowText.Prefix}" Margin="0,0,0,10">
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding FilePrefix, UpdateSourceTrigger=PropertyChanged}" Width="150" Padding="8" FontSize="13" VerticalContentAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="_" FontSize="14" FontWeight="Bold" VerticalAlignment="Center" Margin="5,0"/>
                        <TextBlock Grid.Column="2" Text="{Binding SelectedConfig.Name}" FontSize="13" VerticalAlignment="Center" Foreground="Gray"/>
                    </Grid>
                </GroupBox>
                <GroupBox Grid.Row="1" Header="{x:Static helper:AddConfWindowText.Description}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <TextBlock Text="{Binding SelectedConfigDescription}" TextWrapping="Wrap" Padding="10" FontSize="13"/>
                    </ScrollViewer>
                </GroupBox>
            </Grid>
        </Grid>
        <Border Grid.Row="3" Background="#FFF5F5F5" Padding="10,5" BorderBrush="#FFDDDDDD" BorderThickness="0,1,0,0">
            <TextBlock FontSize="11" Foreground="Gray">
                <Run Text="Selected: "/><Run Text="{Binding SelectedConfig.Name, Mode=OneWay}" FontWeight="SemiBold"/>
            </TextBlock>
        </Border>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="{x:Static helper:AddConfWindowText.Add}" Click="Add_Click" Style="{StaticResource PrimaryButton}" Width="100" IsEnabled="{Binding HasSelectedConfig}"/>
            <Button Content="{x:Static helper:AddConfWindowText.Cancel}" Click="Cancel_Click" Style="{StaticResource ActionButton}" Width="100" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</local:CWindow>