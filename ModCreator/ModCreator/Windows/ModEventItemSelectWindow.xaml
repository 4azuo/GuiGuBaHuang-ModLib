<local:CWindow x:Class="ModCreator.Windows.ModEventItemSelectWindow" x:TypeArguments="localData:ModEventItemSelectWindowData"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:ModCreator.Windows"
        xmlns:localData="clr-namespace:ModCreator.WindowData" mc:Ignorable="d"
        Title="Select Item" Height="600" Width="800" WindowStartupLocation="CenterOwner"
        Icon="/Resources/app-icon.png" FontFamily="Segoe UI">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#FF2E5090" Padding="15">
            <TextBlock Text="{Binding WindowTitle}" FontSize="18" FontWeight="Bold" Foreground="White"/>
        </Border>
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Category:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
            <ComboBox Grid.Column="1" ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory, Mode=TwoWay}" Height="28" FontSize="13"/>
        </Grid>
        <Grid Grid.Row="2" Margin="10,0,10,10">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="🔍" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" BorderBrush="#FFCCCCCC" BorderThickness="1" Padding="8" FontSize="13">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Style.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <VisualBrush Stretch="None" AlignmentX="Left">
                                            <VisualBrush.Visual>
                                                <TextBlock Text="Search by name or description..." Foreground="Gray" Margin="8,0,0,0"/>
                                            </VisualBrush.Visual>
                                        </VisualBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </Grid>
        <Grid Grid.Row="3" Margin="10,0,10,10">
            <Grid.ColumnDefinitions><ColumnDefinition Width="300"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <GroupBox Grid.Column="0" Header="Select Item" Margin="0,0,10,0">
                <ListBox ItemsSource="{Binding FilteredItems}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" DisplayMemberPath="DisplayName" ScrollViewer.HorizontalScrollBarVisibility="Disabled" MouseDoubleClick="ListBox_MouseDoubleClick"/>
            </GroupBox>
            <GroupBox Grid.Column="1" Header="Description">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <TextBlock Text="{Binding SelectedItem.DisplayName}" FontWeight="Bold" FontSize="14" TextWrapping="Wrap" Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding SelectedItem.Description}" TextWrapping="Wrap" FontSize="12" Foreground="#FF666666" Margin="0,0,0,10"/>
                        <TextBlock Text="Code:" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,5"/>
                        <Border Background="#FFF5F5F5" BorderBrush="#FFCCCCCC" BorderThickness="1" Padding="8" CornerRadius="3">
                            <TextBlock Text="{Binding SelectedItem.Code}" TextWrapping="Wrap" FontFamily="Consolas" FontSize="11" Foreground="#FF333333"/>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </GroupBox>
        </Grid>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="OK" Click="OK_Click" Style="{StaticResource PrimaryButton}" Width="100" Margin="0,0,10,0" IsEnabled="{Binding HasSelectedItem}"/>
            <Button Content="Cancel" Click="Cancel_Click" Style="{StaticResource ActionButton}" Width="100"/>
        </StackPanel>
    </Grid>
</local:CWindow>